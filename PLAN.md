# Plan: Vode Plugins — Monorepo Skeleton (Raw VST3 SDK, all-MIT)

**Date:** 2026-08-31
**Status:** COMPLETE — Phases 0–5 implemented; full Release build green; ctest 7/7 pass (vdplg_tests + 6 L3 cases incl. sectra_sine). Sectra Scope plugin: Phases 1–4 done (DSP + VSTGUI editor), see PLAN-sectra-scope.md.
**Brand:** **Vode Plugins** (replaces the working name "vode-vst"). Used as the product/vendor brand everywhere user-facing: plugin vendor string in VST3 factories ("Vode Plugins"), README title, about boxes. **Naming rule (user decision):** no "vode" in folder or variable prefixes — the shared library is simply `common`, and all internal prefixes (CMake options, targets, namespaces, files) use **`VDPLG`** / `vdplg_`. CMake project name: `vdplg`. The on-disk workspace folder remains `d:\Projects\vode-vst` unless the user asks to rename it.

## Implementation log (append-only; keeps plan self-contained across context resets)
- **2026-08-31 (submodules):** All four submodules added & pinned. Actual pins: `vst3sdk` @ tag **`v3.8.1_build_84`** (SHA `3cdf9ca5`) — note the real tag name is `v3.8.1_build_84`, NOT "VST3 SDK 3.8.1"; `signalsmith-dsp` @ **`v1.7.0`**; `catch2` @ **`v3.16.0`**; dr_wav repo is actually **`github.com/mackron/dr_libs`** (the plan's `mackron/dr_wav` URL does not exist) checked out at tag **`wav-0.14.5`** (SHA `fa931f3`), submodule path kept as `third_party/dr_wav`.
- **Nested-submodule quirk:** `git submodule update --init --recursive` from the superproject root hit a transient "Permission denied" writing `.git/modules/third_party/vst3sdk/modules/public.sdk/config` (antivirus/lock). Reliable fix: run `git submodule update --init --force --recursive` **from inside `third_party/vst3sdk`**. `init-working-copy.ps1` must handle this (retry from inside vst3sdk if top-level recursive fails).
- **SDK layout reality (3.8.1):** there is **no `AudioEffectExample`** in this SDK version. Samples live in `public.sdk/samples/vst/` (`again`, `panner`, `adelay`, …). Template used for our passthrough plugin: **`again`** (factory in `source/againentry.cpp` via `BEGIN_FACTORY_DEF`/`DEF_CLASS2`/`END_FACTORY`; processor = `AudioEffect` subclass; controller = `EditController` subclass with `createView` returning nullptr for textless editor).
- **Embedding strategy (decided):** do NOT `add_subdirectory(third_party/vst3sdk)` (its top-level CMakeLists builds all samples + hosting examples + validator — too heavy). Instead in our top-level CMakeLists: `list(APPEND CMAKE_MODULE_PATH <sdk>/cmake/modules)`, `include(SMTG_VST3_SDK)`, set `SDK_ROOT`/`public_sdk_SOURCE_DIR`, call `smtg_setup_platform_toolset()` + `smtg_create_lib_base_target()`, `smtg_create_pluginterfaces_target()`, `smtg_create_public_sdk_common_target()`, `smtg_create_public_sdk_target()` (→ target `sdk`), and `smtg_create_public_sdk_hosting_target()` (→ target `sdk_hosting`, used by vst3testhost). Set before that: `SMTG_CREATE_PLUGIN_LINK=OFF` (no admin symlinks), `SMTG_RUN_VST_VALIDATOR=OFF` (no validator target), `SMTG_ENABLE_VSTGUI_SUPPORT` left OFF (textless editor, no VSTGUI needed). `moduleinfotool` is built manually (small: its sources + `module_win32.cpp`) so bundles get `Contents/Resources/moduleinfo.json`.
- **Plugin packaging facts:** `smtg_add_vst3plugin(<target> <sources>)` requires `PROJECT_VERSION` to be set (our `project(vdplg VERSION 0.1.0)` covers it); Windows bundle lands at `${CMAKE_BINARY_DIR}/VST3/<Config>/passthrough.vst3/Contents/x86_64-win/passthrough.vst3.dll`. `GetPluginFactory` is exported via `SMTG_EXPORT_SYMBOL` (dllexport) on x64 — no `.def` file needed. `smtg_target_configure_version_file(<t>)` generates `projectversion.h` into `PROJECT_BINARY_DIR` (= our build root) and adds it as include dir — shared by all plugins, fine since values come from the single project version.
- **Toolchain on this machine:** cmake is NOT on PATH; VS-bundled cmake 3.31.6 lives at `C:\Program Files\Microsoft Visual Studio\2022\Enterprise\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`. Both PS scripts must locate cmake via vswhere (`-find Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`) with PATH fallback. VS2022 Enterprise with MSVC x64 confirmed present. git 2.44.
- **Signalsmith API notes (v1.7.0):** real headers are at repo ROOT (`filters.h`, `fft.h`, …); `include/signalsmith-dsp/*.h` are thin `#include "../../x.h"` shims. CMake target `signalsmith-dsp` is INTERFACE, include dir = `include/`. Biquad: `signalsmith::filters::BiquadStatic<float>` — configure via `.peak(scaledFreq, gainDb, octaves)` (gain in **dB**), read response via `.responseDb(scaledFreq)` (scaledFreq = f/sampleRate). FFT: `signalsmith::fft::FFT<double> fft(n); fft.fft(in, out)` (complex I/O); `RealFFT` also available. Delay: `signalsmith::delay::Delay<float>(capacity)` with `.write(v).read(delaySamples)`. **No envelope-follower class exists** → L0 "envelope follower steady-state" test replaced by `envelopes::BoxFilter` moving-average steady-state test (constant input ⇒ output == input after warm-up).
- **2026-08-31 (session 1 — plugin + SDK integration gotchas):** VST3 SDK 3.8.1 compile-time traps hit while porting the `again` sample into `plugins/passthrough`: (a) `IAudioProcessor` lives in namespace `Steinberg::Vst` (not `Steinberg`); `int32`/`ParamID` are `Steinberg::`; `FUID` is `Steinberg::FUID`. (b) There is NO `kTrue`/`kFalse` in this SDK — use plain `true`/`false`. (c) `IPtr` has no templated QI helper — use the raw-pointer pattern: `if (auto p = static_cast<Steinberg::Vst::IAudioProcessor*>(this)) p->queryInterface(...)`. (d) `setIoMode` is on `IComponent`, but `setBusArrangements` is on `IAudioProcessor` — call each on the right interface. (e) Parameter points: method is `getPoint` (NOT `getPointInfo`). (f) `IParameterChanges::addParameterData` is pure virtual — any custom impl must implement it. (g) Buffer fill helpers live in `public.sdk/source/vst/vstaudioprocessoralgo.h`. (h) `smtg_target_configure_version_file` writes `projectversion.h` to `${CMAKE_CURRENT_BINARY_DIR}` but only adds `PROJECT_BINARY_DIR` to the include path — plugin CMakeLists must add `${CMAKE_CURRENT_BINARY_DIR}` explicitly. (i) dr_wav single-header: define `DR_WAV_IMPLEMENTATION` in exactly one TU (`common/src/dr_wav_impl.cpp`) and wrap the load/save wrappers in `extern "C" { }` so host/tests link them cleanly. (j) MSVC needs `_USE_MATH_DEFINES` for `M_PI`. (k) Catch2 v3: `Approx` requires `#include <catch2/catch_approx.hpp>` + `using Catch::Approx;`. (l) `tests/l1_passthrough.cpp` instantiates the processor directly (links `Processor.cpp` into the test exe) with a fake `IParameterChanges`/`IParameter` pair.
- **2026-08-31 (session 2 — test infra + L3 debugging, all fixed & verified 6/6):** (a) **Catch2WithMain defines `wmain`, not `main`** on Windows (verified via dumpbin /symbols — only wmain present), but console exes link the CRT which requires `main` → LNK2019. Fix: `tests/src/test_main.cpp` shim (`int main(argc, argv) { return Catch::Session().run(argc, argv); }`) and link plain `Catch2` instead of `Catch2WithMain`. (b) **`sdk_hosting` target omits `module_win32.cpp`** (which contains `Module::create`) → LNK2019 in hosts. Fix: compile `third_party/vst3sdk/public.sdk/source/vst/hosting/module_win32.cpp` directly into `vst3testhost` sources. (c) **JsonParser dangling reference:** member was `const std::string& s_;` bound to the temporary from `ss.str()` → dangled after the full-expression ended → every L3 case failed with "unexpected end of input". Fix: own the string (`std::string s_;`). Lesson: never hold a reference member bound to a temporary. (d) **Phantom golden-compare failure:** `joinPath(caseDir, "")` with an empty `compareGolden` produced `"dir/"` because `isRelative("")` is true → guard all four path resolutions with `!tc.X.empty() &&`. (e) **Biquad `peak()` takes LINEAR gain**, not dB — L0 test expected 6 dB but got 15.56 dB (6 linear). Fix: use `peakDb(scaledFreq, db, octaves)`. (f) **L1 planar-vs-interleaved mismatch:** processor output is planar (channel-major) while test inputs were interleaved (sample-major) → per-sample comparisons failed at specific indices. Fix: `toInterleaved()` helper in the test; compare like-for-like. (g) **`TestParameterChanges::getParameterData` must `addRef()`** before returning — VST3 convention: caller takes ownership of the returned reference. (h) **Processor cached param values went stale:** `gainLinear_`/`mix_` were cached from `AutomatedParameter` but never resynced after `endChanges()` → subsequent no-change blocks used the constructor value (automation ramp stuck at 0.5 peak). Fix: call `updateGainLinear(); updateMix();` after `endChanges()` on ALL FOUR exit paths of `process()` (no-inputs early return, wrong-sample-size return, silence passthrough, main path). Verified: l3_automation_ramp peak = 0.9976, RESULT: PASS.
- **2026-08-31 (final state):** Full Release build green for all targets (base, pluginterfaces, sdk_common, sdk, sdk_hosting, moduleinfotool, common, gen_wavs, passthrough.vst3 bundle, Catch2, vdplg_tests.exe, vst3testhost.exe). ctest: **6/6 pass** — `vdplg_tests` (5648 assertions / 11 test cases: L0 dsp + L1 passthrough) and L3 cases `l3_unity_gain`, `l3_gain_applied`, `l3_mix_zero`, `l3_silence_in`, `l3_automation_ramp`. Plugin artifact: `build\VST3\Release\passthrough.vst3\Contents\x86_64-win\passthrough.vst3.dll`. All debug instrumentation removed (grep `[DBG` clean). Remaining: README refresh, git commits, optional `init-working-copy.ps1` end-to-end validation.
- **2026-08-31 (scripts validated & committed):** `init-working-copy.ps1` hit a **PowerShell 5.1 encoding trap**: the file was UTF-8 *without BOM* and contained em-dashes (`—`) → PS 5.1 decoded it as ANSI, corrupting string literals and causing parse errors ("Unexpected token", "string missing terminator"). Fix: keep both `.ps1` scripts **pure ASCII** (em-dashes replaced with `-`). After fix: first run syncs all submodules + verifies 4 sentinels, exits 0; immediate re-run is fast/idempotent (no fetches), exits 0. All work committed in 7 incremental commits (submodules → scripts → CMake+common → plugin → tests → testhost/testdata → docs).
- **2026-08-31 (Sectra Scope Phase 2 / TDD Red — see PLAN-sectra-scope.md):** New plugin `plugins/sectra-scope` (real-time spectrum analyzer) scaffolded per its plan: full factory/component/controller skeleton with six params (FFT size, window, channel mode, attack, release, dB ref), textless controller with string conversion, plus shared DSP stubs `common/include/vdplg/spectrum.h` + `common/src/spectrum.cpp` whose bodies throw `std::logic_error("not implemented")`. Tests written first: `tests/l0_spectrum.cpp` (28 cases over LogFreqMap/DbScale/ChannelMix/MeterBallistics/BalanceDiff/SpectrumAnalyzer) and `tests/l1_sectra_scope.cpp` (5 cases driving process() incl. all-six-param automation + mid-stream sample-rate change); new L3 case `testdata/cases/sectra_sine.json`. Verified Red (Release): ctest 6/7 — only `vdplg_tests` fails, and exactly the 28 `[l0][sectra]` cases fail with "not implemented" while l1_sectra_scope + all pre-existing suites pass; `l3_sectra_sine` passes already (it gates Phase 5). Gotchas found this session: (a) **header-name collision** — passthrough and sectra-scope both had `Processor.h`; the combined `vdplg_tests` exe includes both Source dirs so `#include "Processor.h"` resolved to the wrong class → renamed sectra's sources to `SectraScopeProcessor.{h,cpp}`. (b) SDK 3.8.1 has **no `str16FromString`**; `TChar`/`String128` are in namespace `Steinberg::Vst` (vsttypes.h), not bare `Steinberg` — param strings are pure ASCII, copied verbatim into UTF-16 by a local helper. (c) Catch2 v3 `Approx` has no `.relative()` — use `.epsilon()`. (d) Testhost gained a `param_count()` metric (controller param count via sdk_hosting) and per-case plugin routing (`sectra_*` cases load sectrascope.vst3; explicit `--plugin` always wins); CTest prepends `l3_` to case names, so case files must NOT carry that prefix. Next: Phase 3 (implement spectrum.cpp → Green) awaits user go-ahead.

- **2026-08-31 (Sectra Scope Phase 3 / TDD Green):** Implemented all six DSP building blocks + SpectrumAnalyzer in common/src/spectrum.cpp (private members added to spectrum.h, public API unchanged). Design: windowed N-sample frame zero-padded to P = nextPow2(>=N*16) before signalsmith ModifiedRealFFT<float> for fine bin resolution (peaks stay aligned to the correct log-frequency column even where the axis is dense; avoids scalloping); coherent-gain factor normFactor = 2/sum(window) makes a full-scale sine read ~A regardless of phase/window; held levels stored in normalized dB with Raw ref applying a constant -3.01 dB offset at display time (live-switchable without re-prime); analyzer uses exactly 720 columns over [20 Hz, 20 kHz] to match the L0 expected-column math; ballistics travel linearly in dB (rise <=24 dB/T_attack, fall <=24 dB/T_release). Processor wiring: two analyzers (A/B), syncAnalyzers(processSetup.sampleRate) refreshes ballistics every block and rebuilds config only when fft/window/ref/sample-rate change, runAnalysis mixes L/R per channel mode then feeds both - analysis is read-only on input buffers so passthrough stays intact. Gotchas: (a) signalsmith method is .fft(in,out) not fftRaw; (b) ProcessData has no sampleRate member - use processSetup.sampleRate; (c) header declarations need fully-qualified Steinberg::Vst::Sample32 / Steinberg::int32. Verified Green (Release): all 28 [l0][sectra] cases pass, l1_sectra_scope + pre-existing suites pass, ctest 7/7 green. Next: Phase 4 (VSTGUI UI) awaits user go-ahead.
- **2026-08-31 (Sectra Scope Phase 4 / VSTGUI editor):** Flipped SMTG_ENABLE_VSTGUI_SUPPORT ON (top-level CMakeLists, smtg_enable_vstgui_support pointing at ${VDPLG_SDK_ROOT}/vstgui4). Live spectra cross-process transport uses the SDK dataexchange utility (NOT custom shared memory): processor owns a DataExchangeHandler (blockSize=scopeDataSize(), numBlocks=8, userContextID=kScopeQueueId from Source/scopedata.h) and sends its two smoothed 720-col dB spectra each block via sendScopeData(); controller implements IDataExchangeReceiver, queueOpened sets dispatchOnBackgroundThread=false (repaints stay on main thread), onDataExchangeBlocksReceived filters by kScopeQueueId, takes blocks[numBlocks-1], feeds scopes_[0] from data->a / others from data->b. Host-mediated queues give automatic multi-instance pairing + IMessage fallback for older hosts. Controller now inherits EditController + IDataExchangeReceiver + VSTGUI::VST3EditorDelegate; createView returns new VST3Editor(this,"view","editor.uidesc"); createCustomView builds ScopeView instances ("scopeA"/"scopeB"). Resource/editor.uidesc: root container 720x560 bg #1e1e1eff fixed size, two custom scope views stacked (each 720x200), six CKnob controls bound to param ids 1..6 (this bundled VSTGUI version has NO CChoiceControl, so discrete params are knobs with matching inc steps). ScopeView (Source/ScopeView.{h,cpp}) draws bg, left dB labels/margin, normal -120..0 grid every 6 dB (labels every 12) or +/-12 balance grid w/ emphasized 0-line in balance mode, octave ticks {20..10000} Hz, filled CGraphicsPath curve + brighter stroked top edge; bin centers plotted at (i+0.5)/720. MSVC x64 compile gotchas hit & fixed: (a) SMTG_OVERRIDE not visible through vstgui/lib/cview.h alone -> plain override in ScopeView.h; (b) std::vector<float> dbA_ (kNumCols,-120.f) is most-vexing-parse -> brace-init dbA_{kNumCols,-120.f}; (c) unqualified DataExchangeReceiverHandler fails name lookup inside our namespace (no file-scope using-directive) -> fully qualified Steinberg::Vst::DataExchangeReceiverHandler dataExchange_{this}; (d) TBool is Steinberg::TBool, IMessage is Steinberg::Vst::IMessage. Verified (Release): full build green, editor.uidesc present in bundle Contents/Resources/, ctest 7/7. Remaining manual check: load sectrascope.vst3 in REAPER and confirm live scopes render. Next: Phase 5 (Integration & docs) awaits user go-ahead.
- **2026-09-01 (Sectra Scope — first real-host debugging, two bugs found via Studio One):** (1) **No UI window:** uidesc had the root as a bare top-level node ("views": {"CViewContainer": ...}). In this VSTGUI version VST3Editor::open() calls description->createView("view", ...) which ONLY searches under a "templates" section for an entry whose key matches the requested name (uidescription.cpp UIDescription::createView); no match -> null view -> nothing shown. Fix: wrap root in "templates" with key "view" (matches SDK dataexchange sample layout). (2) **Host crash on playback start (Studio One, exception 0xc0000374 STATUS_HEAP_CORRUPTION per WER event log):** SpectrumAnalyzer::process() advanced its trailing-N history with history_.begin()+numSamples where history_ is sized fftSize_; hosts delivering blocks LARGER than the FFT size (Studio One's big buffer sizes; REAPER default 512-1024 never triggered it) made that iterator land past end-of-vector -> UB -> heap corruption. Silence blocks skip runAnalysis entirely so the first REAL audio block was also the first analyzer execution - hence "crash when starting playback". Fix (TDD Red->Green): new L0 test "analyzer accepts blocks larger than the FFT size without corruption" (blocks of N/2N/4N, silence finiteness + 440 Hz peak location) failed with "vector too long"; process() now chunks input into <=fftSize frames delegating to private processFrame() (single-frame logic unchanged). Full Release rebuild + ctest 7/7 green. Lesson: L0/L1/L3 all used small blocks, so host-dependent large-block behavior was untested - always consider numSamples > fftSize in any streaming DSP.

- **2026-09-02 (UI screenshot F1 done + exit-time crash fixed — see PLAN-ui-screenshots.md):** Implemented design D1: free function `renderFrameToPng(CFrame*, const std::string&)` in Controller.cpp (COffscreenContext -> frame->draw -> createBitmapMemoryPNGRepresentation -> CFileStream writeRaw, same recipe as VST3Editor::saveScreenshot); `Controller::notify()` intercepts `"vdplg.debug.screenshot"` (attr `path`) via `std::strcmp` — FIDString is `const char*`, never compare with ==; added `attachDebugFrame()/detachDebugFrame()` for tests without a VST3Editor. Three new `[ui]` TEST_CASEs in tests/l1_sectra_scope.cpp (graceful no-op without frame; valid PNG magic + IHDR dimensions with attached frame; notify round-trip through a real Controller). **Also root-caused & fixed the long-standing exit-time heap corruption (0xc0000374)** that crashed any test run once VSTGUI had initialized: cross-TU static destructors (font cache, D2D/WIC COM objects, Win32TaskExecutor message window) tear down in an unsafe order at process exit — fault located via WER ReportArchive + dumpbin disassembly (`mov rax,[rax]` null-deref). Fix: explicit `extern "C" vdplg_tests_shutdown_ui()` called from main() after session.run does `VSTGUI::exit(); CoUninitialize();` while the CRT is still alive (mirrors a real host unloading the plugin DLL). Windows headless-VSTGUI prerequisites documented in PLAN-ui-screenshots.md "Implementation notes": COM apartment BEFORE init (COINIT_APARTMENTTHREADED=2), D2D device registration via temporary hidden HWND + createGraphicsDeviceContext (window destroyed immediately after), explicit shutdown hook, objbase.h include under WIN32_LEAN_AND_MEAN, CRect ctor is (left,top,right,bottom). All diagnostic scaffolding stripped; full Release build clean; ctest 7/7 green. Next: F2 (testhost hidden window + --ui/--screenshot CLI) awaits user go-ahead.
- **2026-09-02 (UI screenshot F2 done - see PLAN-ui-screenshots.md):** vst3testhost gained `--ui` / `--screenshot <path>` CLI plus `"ui"` / `"screenshots":[{"at","file"}]` case fields. `UiCapture` in testhost/src/main.cpp creates a hidden WS_POPUP window (-20000,-20000, 720x560), instantiates the controller from the module factory, connects IConnectionPoint both ways so data-exchange fallback messages reach it during process(), calls createView("editor") with a no-op HostPlugFrame set via setFrame BEFORE attached(hwnd, kPlatformTypeHWND). Screenshots fire between pump blocks via ScreenshotMessage + ctrlCp_->notify; automation mirrored through controller->setParamNormalized. Validation checks PNG magic + IHDR dims (big-endian) == 720x560 + size >= 5KB; failures append [err] to filename and flip exit code. New case testdata/cases/sectra_ui_screenshot.json auto-registered by the existing glob (reconfigure once for ctest pickup); negative check verified (bogus dir -> exit 1, ok:false). **Exit-time crash root cause:** DestroyWindow on the hidden HWND or CoUninitialize() during shutdown while the plugin DLL still holds live VSTGUI state -> AV at process exit (EXITCODE=-1073741819). Fix: skip BOTH in UiCapture::shutdown(); OS reclaims at process end. Also learned: COM must be initialized before Module::create() (plugin static initializers call CoCreateInstance in DllMain); TChar is char16_t not wchar_t; queryInterface takes TUID BY VALUE; IMessage::getMessageID() non-const; real IAttributeList = setInt/getInt/setFloat/getFloat/setString(AttrID,const TChar*)/getString (no count()/getKey()); VST interfaces in Steinberg::Vst namespace. Full suite now **8/8** incl. l3_sectra_ui_screenshot. Next: F3 (time-stamped captures around mode switch, byte-diff two PNGs).
- **2026-09-03 (UI screenshot F3 done - see PLAN-ui-screenshots.md):** New L3 case testdata/cases/sectra_ui_mode_switch.json: sine input, automation "Channel Mode=0.5@0.5s", screenshots at 0.25s (pre-switch L/R labels) and end (post-switch M/S labels), plus new case field "mustDiffer": [...] requiring two captured PNGs to be byte-different. Testhost additions: TestCase::mustDiffer parsing (+case-dir relative resolution), pngFilesDiffer() helper, RunResult::Diff results evaluated after shot validation, JSON "mustDiffer" array + text report lines, folded into exit code. Verified: both PNGs valid 720x560, differ (11113 vs 11034 bytes - label/grid change visible in images), negative check (same file twice -> ok:false, exit 1). Full suite now **9/9** incl. l3_sectra_ui_mode_switch. Note: offline captures show empty scope curves because AnalysisWorker is async (SPSC queue); the difference comes from channel-mode labels + balance grid, which still proves setParamNormalized reached the controller mid-run. Next: F4 golden-image compare (stretch, deferred per plan).
- **2026-09-03 (UI screenshot F4 done - see PLAN-ui-screenshots.md):** Golden-image regression compare. Decoder: miniz tinfl (third_party/miniz tag 3.1.2, only miniz_tinfl.c compiled into common; repo ships no miniz_export.h so CMake writes an empty one to build/generated/; root CMakeLists needed enable_language(C)). API common/include/vdplg/png.h: loadPng (bit depths 8/16, color types 0/2/4/6, all filters, non-interlaced, normalized 8-bit RGBA via tinfl_decompress_mem_to_mem with TINFL_FLAG_PARSE_ZLIB_HEADER), countPixelDiff (per-pixel any-channel > tolerance), compareGoldenUi (diff fraction <= maxFraction). Unit tests tests/l0_png.cpp [ui][l0]: 5 cases / 72 assertions with an in-memory PNG encoder fixture (zlib stored-block framing). Testhost: case-file "goldenUi": {file, ref, maxFraction, tolerance} + CLI --compare-golden-ui ref.png (defaults to last scheduled capture); evaluation folds cmpOk AND withinTolerance into every allOk path; text + JSON reports emit per-compare results. Baseline committed at testdata/golden/sectra_scope_steady.png (steady-state shot; scope curves are empty offline due to async worker, so goldens protect static chrome — keep per-machine if font/DPI differs elsewhere). New L3 case testdata/cases/sectra_ui_golden.json (maxFraction 0.01, tolerance 8) auto-registered via cases glob. Negative check verified: different UI state as ref with maxFraction 0.001 -> FAIL + exit 1 ("pixel diff fraction exceeds limit"). Full suite now **10/10** incl. l3_sectra_ui_golden. All four UI-screenshot features (F1-F4) complete.
## TL;DR
Create a CMake-based C++ monorepo skeleton for the **Vode Plugins** family — multiple VST3 effect plugins on Windows (MSVC/VS2022) — using the **raw Steinberg VST3 SDK 3.8.1 (MIT)** as the plugin framework, **Signalsmith DSP v1.7.0 (MIT)** for DSP building blocks, **Catch2 v3** for L0/L1 unit tests, and a custom **offline VST3 test host (`vst3testhost`)** for L3 integration tests — per INVESTIGATION.md §5-alt, §6.1, §8.3. Dependencies via **git submodules** (pinned commits). Windows-only build for now; repo structured so an osxcross arm64 toolchain can be added later.

## User decisions (from Q&A)
- Framework: **Raw VST3 SDK** (strictly all-MIT), NOT JUCE/iPlug2.
- Scope: minimal plugin + Catch2 unit tests (L0/L1) + `vst3testhost` (L3). No GitHub Actions CI in this plan.
- **Monorepo layout**: separate folder per plugin + a shared "common" part (project hosts multiple plugins).
- macOS: Windows-only for now; keep CMake clean for a future `-DCMAKE_TOOLCHAIN_FILE` osxcross build.
- Dependencies: **git submodules** with pinned commits/tags (changed from FetchContent at user request).

## Verified facts (web, 2026-08-31)
- VST3 SDK: `github.com/steinbergmedia/vst3sdk`, MIT, latest tag **VST3 SDK 3.8.1**. Uses git submodules internally (base, cmake, pluginterfaces, public.sdk, vstgui4, tutorials) → our clone of it as a submodule must be recursive (`git submodule update --init --recursive`) so nested submodules populate. Windows build: `cmake -G "Visual Studio 17 2022" -A x64 ..\vst3sdk`; note `-DSMTG_CREATE_PLUGIN_LINK=0` option to avoid symlink issues.
- Signalsmith DSP: `github.com/signalsmith-audio/dsp`, MIT, header-only C++11, latest **v1.7.0**, headers under `include/signalsmith-dsp/`, provides its own `CMakeLists.txt` (INTERFACE target `signalsmith-dsp`).
- Catch2 v3: `github.com/catchorg/Catch2`, BSL-1.0, CMake-based, `Catch2WithMain` target.
- dr_wav: public domain single-header WAV I/O for the test host (or miniaudio, MIT).

## Proposed repository layout
```
vdplg/                         # brand: "Vode Plugins"; CMake project `vdplg` (workspace dir may stay vode-vst)
├─ setup-dev-env.ps1              # machine-level prerequisites check (see Phase 0)
├─ init-working-copy.ps1          # per-working-copy/branch bootstrap: submodules, build dir (Phase 0)
├─ CMakeLists.txt                 # top-level: project(), C++17, guard on populated submodules, add_subdirectory()
├─ third_party/                   # git submodules (pinned commits)
│  ├─ vst3sdk/                    # github.com/steinbergmedia/vst3sdk @ "VST3 SDK 3.8.1" (has nested submodules)
│  ├─ signalsmith-dsp/            # github.com/signalsmith-audio/dsp @ v1.7.0
│  ├─ catch2/                     # github.com/catchorg/Catch2 @ latest v3.x stable
│  └─ dr_wav/                     # github.com/mackron/dr_wav (public domain, single header)
├─ common/                        # SHARED code across all plugins
│  ├─ CMakeLists.txt              # static lib `common` (DSP helpers, param utils, test helpers)
│  ├─ include/vdplg/              # headers: dsp chain helpers, wav io wrapper, assert/analysis utils
│  └─ src/
├─ plugins/                       # one folder per plugin
│  └─ passthrough/                # first (template) plugin: minimal effect
│     ├─ CMakeLists.txt           # builds <name>.vst3 via SMTG_add_vst3_plugin (SDK macro)
│     └─ Source/
│        ├─ PluginFactory.cpp     # factory + component registration
│        ├─ Processor.h/.cpp      # AudioEffect: passthrough + 1-2 params (e.g., dry/wet gain)
│        └─ Editor.h/.cpp         # minimal editor (VSTGUI or textless stub)
├─ testhost/                      # L3: offline VST3 host (per INVESTIGATION §8.3)
│  ├─ CMakeLists.txt              # exe `vst3testhost`, links vst3sdk pluginterfaces + common
│  └─ src/
│     ├─ main.cpp                 # CLI parsing (§8.3.2)
│     ├─ host/                    # load/instantiate/configure/process lifecycle (§8.3.1)
│     ├─ automation.cpp           # IParameterChanges sample-accurate param changes
│     ├─ wavio.cpp                # dr_wav wrapper
│     └─ analysis/                # peak/RMS/DC/FFT magnitude/THD/golden compare (Signalsmith fft)
├─ tests/                         # L0/L1 Catch2 suite
│  ├─ CMakeLists.txt              # exe `vdplg_tests`, links common + Catch2WithMain; CTest wiring
│  ├─ dsp/                        # Signalsmith component tests (biquad response, envelope follower)
│  └─ processor/                  # instantiate passthrough processor directly, feed buffers
├─ testdata/                      # WAV inputs + JSON test cases for vst3testhost
│  ├─ sine_440.wav, noise.wav, silence.wav
│  └─ cases/*.json                # {input, sampleRate, blockSize, paramAutomation[], assertions[]}
├─ LICENSES/                      # MIT notices for vendored/fetched libs
├─ .gitignore                     # build/, .vs/, etc.
└─ README.md                      # build instructions (VS2022 + CMake), layout explanation
```

## Steps

### Phase 0 — Bootstrap scripts (two, both idempotent & re-run safe)
Two PowerShell scripts at repo root, split by scope. Both must be **safe to re-run at any time**: every step checks current state first and no-ops if already done; re-running on an already-prepared machine/copy changes nothing and exits 0 quickly.

#### 0a. `setup-dev-env.ps1` — machine-level prerequisites (once per machine, not per copy)
Checks/installs nothing automatically — it **detects and reports** what the machine needs, failing fast with actionable messages (non-zero exit) when something is missing:
1. **OS/tooling checks:** Windows + PowerShell ≥ 5.1; `git` on PATH (version ≥ minimum); `cmake` on PATH (≥ 3.21); Visual Studio 2022 with "Desktop development with C++" (MSVC x64) detected via `vswhere` (`-requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64`) or `cl.exe` fallback.
2. **Optional dev tools (warn-only, non-fatal):** a VST3 test host (e.g., REAPER) for manual sanity checks; `git` identity configured (`user.name`/`user.email`) — warn only.
3. **Output:** summary table of each prerequisite → found/missing + version + exact install instruction (VS2022 workload name, cmake.org link, git-scm link). Exit 0 only if all hard requirements pass.
- Idempotency: pure read-only checks, zero side effects — trivially re-run safe.
- Design: `#Requires -Version 5.1`, `Set-StrictMode`, `$ErrorActionPreference = 'Stop'`; flag `-InstallHints` (default on) to print remediation commands.

#### 0b. `init-working-copy.ps1` — per working copy / per branch (run after every clone AND after every checkout/branch switch)
Prepares *this* checkout to match its committed state. Order:
1. **Preflight:** running from repo root (via `$PSScriptRoot` + marker check); `git` available; optionally invoke/assume `setup-dev-env.ps1` passed (flag `-SkipEnvCheck` to skip re-checking; default runs the env check first so the script is self-sufficient).
2. **Working-tree hygiene (branch-switch safety):** if the tree has uncommitted changes in tracked files → warn and abort (never destroy work); submodules are exempt from this check since we manage them.
3. **Submodule sync (the key function):**
   - No `.gitmodules` → skip (pre-first-commit state).
   - `git submodule sync --recursive` (remotes match superproject pins — matters after branch switches that change pins), then `git submodule update --init --recursive --force` (idempotent: already-correct submodules untouched; branch-specific pin changes applied; detached HEAD set to the committed SHA).
   - **Verify population** via sentinel files: `third_party/vst3sdk/pluginterfaces/vst/ivstcomponent.h`, `third_party/signalsmith-dsp/filters.h`, `third_party/catch2/CMakeLists.txt`, `third_party/dr_wav/dr_wav.h`. Any missing after update → print exact remediation command, exit non-zero (catches partial/failed clones).
4. **Build directory prep:** create `build/` if absent; **never delete** an existing build dir. Note: after a branch switch that changes CMake options, a stale cache is possible — detect by comparing a hash of top-level `CMakeLists.txt` stored in `build/.vdplg-cmake-hash` and *warn* (not auto-delete) suggesting `Remove-Item build -Recurse` or `-Reconfigure`.
5. **Optional configure** (flag `-Configure`, default off): `cmake -B build -G "Visual Studio 17 2022" -A x64 <repoRoot>` (CMake configure is idempotent).
6. **Summary:** current branch, `git submodule status` pins, tool versions, next-step hint ("open build in VS / run ctest").

Design constraints (both scripts): `#Requires -Version 5.1` (Windows PowerShell, no pwsh dependency); `Set-StrictMode`; `$ErrorActionPreference = 'Stop'`; all git calls check `$LASTEXITCODE`; network access only in the submodule fetch step when something is actually missing. README documents the workflow: *machine once → `.\setup-dev-env.ps1`; every clone/checkout → `.\init-working-copy.ps1`*.

### Phase 1 — Repo bootstrap & build system
1. Init git repo (if not already), add `.gitignore` (build/, .vs/, out/), flesh out `README.md`.
2. Add git submodules under `third_party/`, pinned to tags:
   ```bat
   git submodule add https://github.com/steinbergmedia/vst3sdk.git third_party/vst3sdk
   git submodule add https://github.com/signalsmith-audio/dsp.git third_party/signalsmith-dsp
   git submodule add https://github.com/catchorg/Catch2.git third_party/catch2
   git submodule add https://github.com/mackron/dr_wav.git third_party/dr_wav
   ```
   Then check out the pinned tags inside each (`vst3sdk` → `VST3 SDK 3.8.1`, `signalsmith-dsp` → `v1.7.0`, `catch2` → latest v3.x stable) and commit the resulting pinned SHAs in the superproject. Document in README: clone with `git clone --recurse-submodules` or `git submodule update --init --recursive` (required because vst3sdk itself contains nested submodules).
3. Top-level `CMakeLists.txt`: `cmake_minimum_required(>=3.21)`, `project(vdplg CXX)`, C++17, options (`VDPLG_BUILD_TESTS=ON`, `VDPLG_BUILD_TESTHOST=ON`), a **guard** that fails configure with a clear message if `third_party/*` are empty (submodules not initialized), then `add_subdirectory(third_party/vst3sdk)` (or its `public.sdk` entry point as required by the SDK's CMake layout), `add_subdirectory(third_party/signalsmith-dsp)`, `add_subdirectory(third_party/catch2)` (tests only), and our dirs: `common`, `plugins/passthrough`, `tests`, `testhost`.
4. Verify: `git submodule update --init --recursive && cmake -B build -G "Visual Studio 17 2022" -A x64 && cmake --build build --config Release` configures cleanly with no targets yet beyond common.

### Phase 2 — Common library
5. `common/` static lib **`common`**: WAV I/O wrapper (dr_wav), small DSP utilities (dB conversions, block helpers), analysis math shared by tests and testhost (RMS/peak/DC/FFT-magnitude via Signalsmith `fft::FFT`). Headers under `include/vdplg/`, namespace `vdplg`. Keep it dependency-light so both plugin tests and the host can link it.

### Phase 3 — First plugin (template for all future plugins)
6. `plugins/passthrough/`: port the VST3 SDK's **AudioEffectExample** structure (factory → `IComponent`/`IAudioProcessor` + `IEditController`) into our folder. Processor: stereo passthrough with 2 parameters (e.g., `Gain` in dB, `Mix` dry/wet) to exercise parameter plumbing, state save/load, and sample-accurate param changes. Factory metadata uses brand vendor string **"Vode Plugins"** and unique ClassID (UUID). Editor: minimal VSTGUI editor showing the two params (SDK ships VSTGUI4; keep it trivial — a textless editor is acceptable fallback if VSTGUI setup fights us).
7. Build target produces `passthrough.vst3` bundle (Windows: `.vst3` folder with Contents/x86_64-win). Use the SDK's CMake plugin macros (`SMTG_add_vst3_plugin` / `SMTG_add_vst3_helper_library`) from `public.sdk/source`.
8. Smoke check: load in any local VST3 host (Reaper) OR immediately via `vst3testhost` once Phase 5 lands.

### Phase 4 — L0/L1 unit tests (Catch2)
9. `tests/` exe `vdplg_tests` linking `common` + `Catch2WithMain`; `enable_testing()` + `add_test` at top level.
   - L0: Signalsmith biquad frequency response vs theory (±0.1 dB at peak), envelope follower steady-state gain, delay line round-trip.
   - L1: instantiate the passthrough processor class directly (link its sources into the test exe or a small static lib), feed synthetic buffers through `process()`, assert: unity-gain passthrough sample-exact, gain param applied correctly, silence-in → silence-out, no NaN.
10. Verify: `ctest --test-dir build -C Release` passes.

### Phase 5 — vst3testhost (L3 offline host)
11. Implement per INVESTIGATION §8.3.1 lifecycle: LoadLibrary the built `.vst3` binary → `GetPluginFactory()` → instantiate component → `initialize(hostContext)` → bus arrangement (stereo) → `setIoMode(kOfflineProcessing)` + `setupProcessing(ProcessSetup{44100, 512, kSample32, kOffline})` → `setActive/setProcessing(true)` → block loop filling `AudioBuffer32` from WAV → `process()` → collect output → teardown.
12. CLI per §8.3.2: `vst3testhost <plugin.vst3> --input x.wav [--blocksize N] --automation "param=value@time,..." --output out.wav --assert "..." [--compare-golden ref.wav --tolerance t] [--report json]`. Param names resolved to indices by enumerating `IParameter`s. Automation applied via `IParameterChanges::addPoint(index, normalized, sampleOffset)`.
13. Built-in analyzers (reuse `common`): peak, RMS, DC offset, FFT magnitude at given Hz, THD, golden compare (float32, tolerance-based primary gate).
14. Wire as CTest invocations over `testdata/cases/*.json`: passthrough unity-gain, gain automation mid-file, silence-in, clip-in sanity (no NaN), determinism (run twice, sample-exact).
15. Verify: full `ctest` run green; JSON report artifact produced.

## Dependencies between steps
- Phase 0 (both scripts) is written alongside Phase 1 (they reference the submodules and sentinels created in Phase 1); they block nothing else but must land before any "verify" step that assumes a ready working copy.
- Step 1–4 (Phase 1) block everything else.
- Phase 2 (common) blocks Phases 4 & 5 (both link it); Phase 3 is independent of Phase 2 (can run in parallel).
- Phase 5 step 15 needs Phase 3's built `.vst3` and Phase 2's analysis utils.
- Parallelizable: Phase 3 ∥ Phase 2; within Phase 5, wavio/analysis ∥ host lifecycle.

## Relevant files / references
- `INVESTIGATION.md` — §6.1 (VST3 SDK skeleton), §8.3 (testhost design, chosen L3), §7 (future osxcross path).
- VST3 SDK `public.sdk/source/pluginsamples/AudioEffectExample` — template for factory/processor/editor structure.
- VST3 SDK CMake macros (`SMTG_add_vst3_plugin` / `SMTG_add_vst3_helper_library`) — reuse instead of hand-rolling bundle packaging.
- Signalsmith headers: `include/signalsmith-dsp/filters.h` (Biquad), `fft.h` (FFT), `envelopes.h` — used by common lib + tests.
- Reference hosts for lifecycle edge cases: `Tracktion/pluginval` (init order), `iffyloop/EasyVst` (lightweight hosting glue) — read-only references, not dependencies.

## Verification
1. Fresh-clone simulation: `git clone --recurse-submodules <repo>` into a temp dir → run `.\init-working-copy.ps1` → exits 0, summary shows branch + submodule pins.
2. Idempotency: run `.\init-working-copy.ps1` again immediately → exits 0 fast, no fetches, no files modified, existing `build/` untouched.
3. Partial-state recovery: `git submodule deinit third_party/catch2` (or delete a sentinel file's dir) → `.\init-working-copy.ps1` detects, re-inits, exits 0.
4. Branch-switch scenario: create a temp branch that bumps one submodule pin → `git checkout <branch>` → `.\init-working-copy.ps1` syncs only the changed submodule, warns about stale CMake cache hash, exits 0.
5. `setup-dev-env.ps1` on this machine exits 0 with full summary; simulate a missing tool (e.g., temporarily hide `cmake` from PATH in a subshell) → non-zero exit with the exact install hint.
6. `cmake -B build -G "Visual Studio 17 2022" -A x64` (or `.\init-working-copy.ps1 -Configure`) configures with zero errors/warnings about missing deps.
7. `cmake --build build --config Release` produces `passthrough.vst3` and `vst3testhost.exe`.
8. `ctest --test-dir build -C Release` — all L0/L1 Catch2 tests + L3 testhost JSON cases pass.
9. Manual: load `passthrough.vst3` in Reaper (or any local VST3 host), confirm it appears, params move, audio passes through.
10. `vst3testhost passthrough.vst3 --input testdata/sine_440.wav --output out.wav --assert "peak_out <= 0dBFS"` exits 0 with JSON report.

## Decisions
- **Brand = "Vode Plugins"** (user request, replaces working name "vode-vst"): VST3 vendor string "Vode Plugins", README/about branding. **No "vode" in folder/variable prefixes** (user request): shared lib is just `common`; all internal prefixes use `VDPLG`/`vdplg_` — CMake project `vdplg`, options `VDPLG_BUILD_TESTS`/`VDPLG_BUILD_TESTHOST`, test exe `vdplg_tests`, headers `include/vdplg/`, namespace `vdplg`, hash file `.vdplg-cmake-hash`.
- **Raw VST3 SDK over JUCE/iPlug2** — user chose strictly all-MIT stack.
- **No UI investment**: minimal VSTGUI editor (or textless) — UI is out of scope for the skeleton; the point is the multi-plugin architecture.
- **Windows-only build now**; CMake stays toolchain-file-friendly for later osxcross arm64 (no WSL setup in this plan).
- **Git submodules** (pinned commits in `third_party/`) over FetchContent — user preference; explicit pins, no network at configure time.
- **Two-script bootstrap split** (user request): `setup-dev-env.ps1` = machine-level prerequisites (read-only checks, once per machine); `init-working-copy.ps1` = per-clone/per-branch state (submodule sync, build dir, optional configure). Both idempotent.
- **First plugin = "passthrough"** — deliberately trivial effect that still exercises params/state/automation; serves as the copy-paste template for real plugins.
- **Excluded from this plan**: GitHub Actions CI, osxcross/WSL setup, ChowDSP (add later if needed), AU/VST2/CLAP wrappers, code signing/notarization.

## Further considerations
1. vst3sdk contains **nested submodules** (base, cmake, pluginterfaces, public.sdk, vstgui4) — a plain `git clone` of our repo leaves them empty; README must mandate `--recurse-submodules`, and the CMake guard (Phase 1 step 3) should detect this and print the exact fix command.
2. Should `vst3testhost` also run against future plugins automatically (glob `plugins/*/*.vst3` + per-plugin `testdata`)? Recommend yes — add a CTest loop over discovered plugin bundles once a second plugin exists.
3. Plugin naming/IDs: each plugin needs a unique VST3 ClassID (UUID). Convention to adopt: generate one UUID per plugin at creation, store in its `PluginFactory.cpp`; document in README so new plugins don't collide.
4. Submodule update workflow: bumping a dep = `cd third_party/<dep> && git fetch --tags && git checkout <tag>` then commit the new pin in the superproject. Document in README.
